import 'package:esnya_shared_resources/core/core.dart';
import 'package:flutter/material.dart';

class CalorieSourcesTable extends StatelessWidget {
  final Map<NutrientType, Amount> computedNutrientAmounts;
  const CalorieSourcesTable({
    Key? key,
    required this.computedNutrientAmounts,
  }) : super(key: key);

  @override
      Widget build(BuildContext context) {
    return Container();

    dsasadi ds adjasasdj 
  }
}
